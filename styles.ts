import { StyleSheet } from 'react-native';

export const globalStyles = StyleSheet.create({
  input: {
    borderWidth: 3,
    borderColor: 'blue',
    padding: 10,
    marginBottom: 20,
    borderRadius: 5,
  },
  title: {
    fontSize: 18,
    fontWeight: 'bold',
    color: 'blue'
  },
});
